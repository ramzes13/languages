
class Test {
    static getInfoAvailableInputs() {
        return [];
        // return this.mapper.map(input => {
        //     return {'slug': input.slug, 'label': input.label}
        // })
    }
}

export default Test;